import React from 'react';
import { GeneratedImage } from '../types';

interface MobileModalProps {
  isOpen: boolean;
  image: GeneratedImage | null;
  onEdit: () => void;
  onDownload: () => void;
  onNewImage: () => void;
}

const MobileModal: React.FC<MobileModalProps> = ({ isOpen, image, onEdit, onDownload, onNewImage }) => {
  if (!isOpen || !image) return null;

  return (
    <div id="mobileModal" className="mobile-modal fixed inset-0 bg-black/80 backdrop-blur-sm flex items-center justify-center z-50 lg:hidden">
      <div className="modal-content bg-brand-primary rounded-lg shadow-xl w-[90vw] max-w-md p-4 flex flex-col gap-4">
        <img id="modalImage" src={image.src} alt={image.prompt} className="modal-image rounded-md w-full" />
        <div className="modal-actions grid grid-cols-3 gap-2">
          <button className="modal-btn edit bg-brand-border hover:bg-brand-border/70 text-white font-semibold py-3 rounded-md flex items-center justify-center gap-2" onClick={onEdit}>
            ✏️ <span className="hidden sm:inline">Editar</span>
          </button>
          <button className="modal-btn download bg-brand-border hover:bg-brand-border/70 text-white font-semibold py-3 rounded-md flex items-center justify-center gap-2" onClick={onDownload}>
            💾 <span className="hidden sm:inline">Salvar</span>
          </button>
          <button className="modal-btn new bg-brand-accent hover:bg-brand-accent-hover text-white font-semibold py-3 rounded-md flex items-center justify-center gap-2" onClick={onNewImage}>
            ✨ <span className="hidden sm:inline">Nova</span>
          </button>
        </div>
      </div>
    </div>
  );
};

export default MobileModal;